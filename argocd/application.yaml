apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unum
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/danielriddell21/k8s-unum
    targetRevision: trunk
    path: .
    directory:
      recurse: true
      exclude: '{.github/**,terraform/**}'
  destination:
    server: https://kubernetes.default.svc
    namespace: unum
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
