apiVersion: apps/v1
kind: Deployment
metadata:
  name: unum-json
  namespace: unum
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unum-json
  template:
    metadata:
      labels:
        app: unum-json
    spec:
      containers:
        - name: unum-json
          image: ghcr.io/danielriddell21/unum:latest
          args: ["json", "--web"]
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 10
