# Placeholder. After the first `terraform apply` creates the fiatlux tunnel,
# regenerate this with:
#
#   just seal-secrets   # select "cloudflared-fiatlux"
#
# which reads $(terraform output -raw fiatlux_tunnel_token) and writes the
# real encrypted token here.
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: cloudflared-token
  namespace: fiatlux
spec:
  encryptedData:
    token: REPLACE_ME_WITH_kubeseal_OUTPUT
  template:
    metadata:
      name: cloudflared-token
      namespace: fiatlux
