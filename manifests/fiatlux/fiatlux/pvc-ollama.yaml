apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: fiatlux
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
