apiVersion: apps/v1
kind: Deployment
metadata:
  name: unum-hash
  namespace: unum
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unum-hash
  template:
    metadata:
      labels:
        app: unum-hash
    spec:
      containers:
        - name: unum-hash
          image: ghcr.io/danielriddell21/unum:latest
          args: ["hash", "--web"]
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 10
